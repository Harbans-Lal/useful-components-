/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['www.shutterstock.com'], // Add the Shutterstock domain here
  },
  reactStrictMode: true,
  
};

export default nextConfig;
